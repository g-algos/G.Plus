<Window x:Class="GPlus.UI.Views.ProgressBar"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
         xmlns:local="clr-namespace:GPlus.UI.Views"
        xmlns:converters="clr-namespace:GPlus.UI.Helpers.Converters"
         xmlns:controls="clr-namespace:G.wpf.Controls;assembly=G.wpf"
         xmlns:contents="clr-namespace:GPlus.Base.Resources.Localizations;assembly=GPlus.Base" 
         mc:Ignorable="d"
         Title="{x:Static contents:Content.TakeABreak}" SizeToContent="WidthAndHeight" ResizeMode="NoResize" Topmost="True" WindowStartupLocation="CenterScreen">
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/G.wpf;component/Themes/Generic.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>
    <Grid  Margin="{StaticResource PaddingSM}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" Style="{StaticResource LabelMedium}" TextWrapping="Wrap"
           Margin="{StaticResource PaddingSM}"
           Text="{x:Static contents:Content.BreakInProgress}"/>
        
        <ProgressBar Grid.Row ="1" x:Name="PBar"
                     Margin="{StaticResource PaddingSM}"
                     Height="30"
                     Minimum="0"
                     Maximum="100"
                     Value="0" />
        <Button Grid.Row="2" Style="{StaticResource PrimaryButton}" Margin="8,16,8,16" Content="{x:Static contents:Content.Ok}"
                HorizontalAlignment="Right" Click="Ok_Click"/>
    </Grid>
</Window>
