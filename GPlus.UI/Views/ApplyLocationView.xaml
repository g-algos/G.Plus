<Window x:Class="GPlus.UI.Views.ApplyLocationView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:GPlus.UI.Views"
        xmlns:converters="clr-namespace:GPlus.UI.Helpers.Converters"
        xmlns:commands="clr-namespace:G.wpf.Commands;assembly=G.wpf"
        xmlns:controls="clr-namespace:G.wpf.Controls;assembly=G.wpf"
        xmlns:contents="clr-namespace:GPlus.Base.Resources.Localizations;assembly=GPlus.Base"  
        mc:Ignorable="d"
        Title="{x:Static contents:Content.SelectScheme}" Height="Auto" Width="200" MaxHeight="200" ResizeMode="NoResize"
        WindowStartupLocation="Manual">
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/G.wpf;component/Themes/Generic.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <controls:GCheckListBox 
          Grid.Column="0" 
          Margin="{StaticResource PaddingXS}"
          ItemsSource="{Binding Localizations}" 
          DisplayMemberPath="Name"
          CheckedItems="{Binding SelectedLocalization}" />
        <!-- Footer -->
        <Grid DockPanel.Dock="Bottom" Margin="{StaticResource PaddingSM}" Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Column="0" VerticalAlignment="Center">
             <Hyperlink Style="{StaticResource LinkLarge}"
                        Command="{x:Static commands:GlobalCommands.OpenUriCommand}" 
                        CommandParameter="http://g-algos.com/G.plus/LocalizationSchemas/#apply-scheme-to-view">
                        Learn more
            </Hyperlink>
            </TextBlock>
            <Button Grid.Column="1" Content="OK" IsDefault="True" Style="{StaticResource PrimaryButton}" Command="{Binding ApplyCloseCommand}"/>
        </Grid>

    </Grid>
</Window>
